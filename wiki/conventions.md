# Wiki Conventions — [COMPANY X] ([TICKER])

Decisions the librarian has made about wiki organization, cross-referencing, and taxonomy. New librarian instances read this file FIRST on every spawn — before `index.md`, before any wiki page — to inherit the accumulated taste of every prior librarian.

This is the Wikipedia Manual of Style pattern applied to a stateless agent loop. The librarian dies every call, but its judgment lives on in this file.

**Rules for this file:**
- Append-only. Do not delete old entries.
- When a rule is reversed, mark the old entry `[superseded YYYY-MM-DD: <reason>]` and leave it in place. History is knowledge.
- Every new entry includes a date tag so the librarian can see which decisions are recent.
- Keep each entry to one line or one short bullet. This file is meant to be fully read on every spawn; keep it scannable.
- Group entries by category. Add new categories as needed.

---

## Taxonomy

*(Rules about which directory new topics go in. Example: "Wearables findings go under `segments/reality_labs/`, not a new `segments/wearables/` page. Reason: Reality Labs is the reporting segment; wearables are a product line within it.")*

- *(no rules yet)*

## Cross-references

*(Rules about how pages link to each other. Example: "Cross-references use relative markdown paths, e.g. `[ARPP](../drivers/arpp.md)`. Never absolute paths or wikilinks.")*

- *(no rules yet)*

## Supersession handling

*(Rules about what to do when new information supersedes old. Example: "Superseded quantified claims are moved to a `## Previously` subsection at the bottom of the page with their original source. Never deleted.")*

- *(no rules yet)*

## Page naming

*(Rules about how to name new wiki files. Example: "Driver pages use the parameter name in lowercase with underscores: `drivers/arpp_growth_yr1_3.md`. Risk pages use short descriptive slugs: `risks/china_concentration.md`.")*

- *(no rules yet)*

## Section structure

*(Rules about how to organize content within a page. Example: "Every driver page has these sections in order: 'Current Calibration', 'Drivers', 'Risks', 'Sources', 'Cross-references', and optionally 'Previously'.")*

- *(no rules yet)*

## Contradiction structure

*(Rules about how to present contradictions between sources. Example: "When two sources disagree, use a `## Conflicting claims` subsection with bullet points: `- Source A: {claim}` / `- Source B: {claim}`. Do not paraphrase into a synthesis.")*

- *(no rules yet)*
